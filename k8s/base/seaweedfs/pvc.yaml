apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: seaweedfs-pvc
  namespace: charisma
  labels:
    app.kubernetes.io/name: seaweedfs
    app.kubernetes.io/part-of: charisma
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
