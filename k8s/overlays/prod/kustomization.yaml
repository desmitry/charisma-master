apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: charisma

resources:
  - ../../base

patches:
  - path: patches/api-gateway.yaml
  - path: patches/ml-worker.yaml
  - path: patches/frontend.yaml
